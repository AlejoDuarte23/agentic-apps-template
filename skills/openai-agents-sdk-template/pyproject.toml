[project]
name = "openai-agents-sdk-template-app"
version = "0.1.0"
description = "Minimal OpenAI Agents SDK app using the SDK default OpenAI model behavior."
requires-python = ">=3.11"
dependencies = [
    "openai-agents",
    "python-dotenv",
]

[project.scripts]
agent-template = "app:main"
